name: hello-world
on:
  push:
   branches:
     -main
  pull_request:
   branches:
     -main
  workflow_dispatch:
jobs:
  hello-job:
    runs-on: ubuntu-latest
    steps:
      - name: hello world
        run: echo "Hello World!"
        shell: bash
  goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: goodbye world
        run: echo "Goodbye World!"
        shell: bash
  
     
